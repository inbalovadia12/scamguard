{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user"
    },
    "subscription_plan": {
      "type": "string",
      "enum": [
        "starter",
        "plus",
        "premium"
      ],
      "default": "starter",
      "description": "The user's subscription plan"
    },
    "subscription_status": {
      "type": "string",
      "enum": [
        "inactive",
        "active",
        "canceled"
      ],
      "default": "inactive",
      "description": "Current subscription status"
    },
    "credits_used": {
      "type": "number",
      "default": 0,
      "description": "AI credits used this month"
    },
    "credits_reset_month": {
      "type": "string",
      "description": "Month when credits were last reset (YYYY-MM format)"
    },
    "onboarding_completed": {
      "type": "boolean",
      "default": false,
      "description": "Whether the user has completed onboarding"
    },
    "onboarding_goals": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Goals selected during onboarding"
    },
    "alert_preference": {
      "type": "string",
      "enum": [
        "all",
        "high_risk_only",
        "financial_only"
      ],
      "default": "all",
      "description": "User's alert preference from onboarding"
    },
    "notify_email": {
      "type": "boolean",
      "default": true,
      "description": "Whether to send email notifications for family alerts"
    },
    "privacy_auto_redact": {
      "type": "boolean",
      "default": true,
      "description": "Whether to auto-redact PII in stored messages"
    },
    "kid_mode": {
      "type": "boolean",
      "default": false,
      "description": "Whether kid mode is enabled (simpler language, bigger text, kid-focused scam protection)"
    },
    "call_guard_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether the Call Guard add-on is enabled for this user"
    },
    "call_guard_status": {
      "type": "string",
      "enum": [
        "none",
        "active",
        "cancelled",
        "expired"
      ],
      "default": "none",
      "description": "Current Call Guard add-on entitlement status"
    },
    "call_guard_phone_number": {
      "type": "string",
      "description": "Normalized phone number (digits only) that Retell screens for this user"
    },
    "call_guard_subscription_id": {
      "type": "string",
      "description": "PayPal subscription ID for the Call Guard add-on"
    },
    "call_guard_expires_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the Call Guard entitlement expires or was disabled"
    },
    "call_guard_notifications": {
      "type": "boolean",
      "default": true,
      "description": "Whether the user receives notifications for Call Guard verdicts"
    }
  },
  "required": [
    "role"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "$or": [
        {
          "id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}